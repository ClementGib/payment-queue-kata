spring:
  application:
    name: payment-queue-kata
  datasource:
    url: jdbc:postgresql://localhost:15432/pqkdb
    username: pqkadm
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql